// dependancies to read JSON
const fs = require('fs');

// calls uuid to make unique ID numbers
const uuid = require('../heplers/uuid');

// calls router from 
const router = require('express').Router();